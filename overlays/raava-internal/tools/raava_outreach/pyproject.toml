[project]
name = "raava-outreach"
description = "Raava outreach loop CLI surface — subprocess shell-out to the raava-outreach binary"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.centaur]
module = "client.py"
secrets = []
