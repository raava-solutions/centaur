[project]
name = "websearch"
description = "Web search and deep research via Exa plus configurable synthesis"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.54.0",
    "httpx>=0.28.0",
    "pydantic>=2.10.0",
    "python-dotenv>=1.0.0",
    "rich>=13.0.0",
    "typer>=0.12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.centaur]
module = "client.py"
secrets = [
    {type = "http", name = "EXA_API_KEY", match_headers = ["x-api-key"], hosts = ["api.exa.ai"]},
    {type = "http", name = "OPENROUTER_API_KEY", match_headers = ["Authorization"], hosts = ["openrouter.ai"]},
    {type = "http", name = "ANTHROPIC_API_KEY", match_headers = ["x-api-key"], hosts = ["api.anthropic.com"]},
]
