[project]
name = "firecrawl"
description = "Firecrawl search and single-page scrape"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.0",
    "python-dotenv>=1.0.0",
    "rich>=13.0.0",
    "typer>=0.12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.centaur]
module = "client.py"
secrets = [
    {type = "http", name = "FIRECRAWL_API_KEY", match_headers = ["Authorization"], hosts = ["api.firecrawl.dev"]},
]

